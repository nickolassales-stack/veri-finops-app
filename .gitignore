# ---- Segredos: NUNCA versionar ----
.env
.env.*
!.env.example
*.pem
*.key
*.ppk
id_rsa*
id_ed25519*
*.pub
secrets/

# `credentials` SEM barra casa com qualquer arquivo OU DIRETORIO de mesmo nome,
# em qualquer profundidade. Assim escrito, engoliu
# `web/src/app/api/admin/accounts/[accountId]/credentials/` -- duas rotas de API
# ficaram fora de dois commits sem que `git add` ou `git status` dissessem nada,
# e o portal foi para producao respondendo 404 na tela de credenciais.
#
# Ancorado na raiz e nos caminhos reais em que um arquivo de segredo aparece.
/credentials
**/.aws/credentials
**/credentials.json

# ---- Node / Next.js ----
node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ---- Testes / coverage ----
coverage/
.nyc_output/
playwright-report/
test-results/

# ---- Python (ETL) ----
__pycache__/
*.py[cod]
venv/
.venv/
.pytest_cache/

# ---- Snapshots de inspecao (podem conter dados de custo) ----
# O snapshot revisado e versionado manualmente em docs/schema-snapshot.md
scripts/out/

# ---- Arquivos operacionais, proprios de cada maquina ----
# VERSAO-IMPLANTADA.txt registra qual commit esta no ar NAQUELA maquina. E um
# fato sobre a maquina, nao sobre o repositorio: versiona-lo faria o registro de
# uma maquina sobrescrever o da outra a cada pull, e o arquivo passaria a dizer
# o que esta no REPOSITORIO em vez do que esta NO AR -- exatamente a pergunta
# que ele existe para responder.
VERSAO-IMPLANTADA.txt

# ---- Coletor OVH (POC) ----
# `out/` ja e coberto pela regra generica acima, mas fica explicito aqui: o
# resultado da POC e sanitizado, e ainda assim revela quanto a empresa gasta e
# em que. Nunca versionar.
scripts/ovh-collector/out/
scripts/ovh-collector/venv/

# ---- SO / editores ----
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json

# ---- Temporarios do Office ----
~$*.docx
~$*.xlsx
